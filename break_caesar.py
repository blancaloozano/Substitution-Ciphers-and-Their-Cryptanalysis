
from collections import Counter
from caesar import decrypt

def chi_squared(text: str, table: dict[str, float]) -> float:

def break_caesar(chiphertext: str, langauge: str = "en") -> tuple[int, str]:
    